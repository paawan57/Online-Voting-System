#!/usr/bin/env node

"use strict";
var path = require('path');
var fs = require('fs');
var p = fs.realpathSync(__filename);
var lib = path.join(path.dirname(p), '../lib');
console.log('local = ' + p);
require(lib+'/uppercase.js').convert();